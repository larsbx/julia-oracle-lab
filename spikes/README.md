# Spikes

Every spike records its question, assumptions, pinned inputs, seed, precision, dependency lock, evidence budget, and promotion-or-deletion criterion.

Spikes are never registered as durable oracles and are never imported by proof, certificate-acceptance, authorization, production, or deployment paths.
