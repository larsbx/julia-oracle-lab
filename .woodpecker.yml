steps:
  estate-audit:
    image: python:3.12-slim
    commands:
      - python tools/audit_estate_layout.py

  test:
    image: julia:1.11
    commands:
      - julia --project=. -e 'using Pkg; Pkg.instantiate(); Pkg.test()'

when:
  event:
    - push
    - pull_request
